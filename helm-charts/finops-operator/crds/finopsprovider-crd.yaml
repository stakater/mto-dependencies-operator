apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: finopsproviders.finops.stakater.com
spec:
  group: finops.stakater.com
  names:
    kind: FinOpsProvider
    listKind: FinOpsProviderList
    plural: finopsproviders
    singular: finopsprovider
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FinOpsProvider is the Schema for the finopsproviders API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              ProviderOptions holds provider-specific configuration options.
              Exactly one of AWS, GCP, Azure, or OnPrem must be set.
              These validations operate on the Go field names (AWS, GCP, Azure, OnPrem).
              Se https://opencost.io/docs/configuration/ for possible options
              todo: +kubebuilder:validation:XValidation:rule="has(self.Aws) || has(self.Gcp) || has(self.Azure) || has(self.OnPrem)", message="At least one provider option (awsoptions, gcpoptions, azureoptions, onpremoptions) must be set"
              todo: +kubebuilder:validation:XValidation:rule="(has(self.Aws) ? 1 : 0) + (has(self.Gcp) ? 1 : 0) + (has(self.Azure) ? 1 : 0) + (has(self.OnPrem) ? 1 : 0) == 1", message="Exactly one provider option (awsoptions, gcpoptions, azureoptions, onpremoptions) must be set"
            properties:
              awsoptions:
                description: AWS specific options.
                properties:
                  cloudIntegrationSecret:
                    description: CloudIntegrationSecret is the Azure Subscription
                      ID.
                    type: string
                  pricingModelSource:
                    description: |-
                      PricingModelSource indicates how the pricing model is provided to OpenCost.
                      e.g., "Pricebook" if derived from PriceBook CRs.
                    type: string
                type: object
              azureoptions:
                description: Azure specific options.
                properties:
                  cloudIntegrationSecret:
                    description: CloudIntegrationSecret is the Azure Subscription
                      ID.
                    type: string
                  pricingModelSource:
                    description: |-
                      PricingModelSource indicates how the pricing model is provided to OpenCost.
                      e.g., "Pricebook" if derived from PriceBook CRs.
                    type: string
                type: object
              gcpoptions:
                description: GCP specific options.
                properties:
                  cloudIntegrationSecret:
                    description: CloudIntegrationSecret is the Azure Subscription
                      ID.
                    type: string
                  pricingModelSource:
                    description: |-
                      PricingModelSource indicates how the pricing model is provided to OpenCost.
                      e.g., "Pricebook" if derived from PriceBook CRs.
                    type: string
                type: object
              onpremoptions:
                description: OnPrem specific options.
                properties:
                  pricingModelSource:
                    description: |-
                      PricingModelSource indicates how the pricing model is provided to OpenCost.
                      e.g., "Pricebook" if derived from PriceBook CRs.
                    type: string
                type: object
            type: object
          status:
            description: FinOpsProviderStatus defines the observed state of FinOpsProvider.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the FinOpsProvider's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastSyncTime:
                description: LastSyncTime is the timestamp of the last successful
                  sync of OpenCost configuration.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed spec.
                format: int64
                type: integer
            type: object
        type: object
        x-kubernetes-validations:
        - message: FinOpsProvider must be named 'default' (singleton).
          rule: self.metadata.name == 'default'
        - message: At least one provider option (awsoptions, gcpoptions, azureoptions,
            onpremoptions) must be set
          rule: has(self.spec.awsoptions) || has(self.spec.gcpoptions) || has(self.spec.azureoptions)
            || has(self.spec.onpremoptions)
        - message: Exactly one provider option (awsoptions, gcpoptions, azureoptions,
            onpremoptions) must be set
          rule: '(has(self.spec.awsoptions) ? 1 : 0) + (has(self.spec.gcpoptions)
            ? 1 : 0) + (has(self.spec.azureoptions) ? 1 : 0) + (has(self.spec.onpremoptions)
            ? 1 : 0) == 1'
    served: true
    storage: true
    subresources:
      status: {}

